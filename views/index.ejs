<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CookSeek</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="apple-touch-icon" sizes="180x180" href="assets/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="assets/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="assets/favicon-16x16.png">
    <link rel="manifest" href="assets/site.webmanifest">
</head>
<body>
    <nav class="navbar bg-body-tertiary">
        <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="assets/android-chrome-192x192.png" alt="Logo" width="30" height="24" class="d-inline-block align-text-top">
            CookSeek
        </a>
        
        </div>
    </nav>
    <div class="container">
        <form method="post"  action="/getrecipes">
            <div class="mb-3">
                <label for="exampleFormControlTextarea1" class="form-label">Enter the ingredients or items in your fridge</label>
                <textarea name="ingredients" class="form-control" id="exampleFormControlTextarea1" rows="3" placeholder="Seperate each item with a comma(,). Ex: Eggs, Tomatoes, Potatoes, etc"></textarea>
            </div>
            <div class="d-grid gap-2">
                <button class="btn btn-primary" type="submit">Get Recipes</button>
              </div>
        </form>
      </div>
      <% if (locals.ingredients) {%>
        <div class="container">
            <% for (var i = 0; i < ingredients.length; i++) { %>
                <div class="container">
                    <h1 class="display-6"><%= ingredients[i].name %></h1>
                    <img src=<%= ingredients[i].image %> />
                    <ul>
                        <% for (j = 0; j < ingredients[i].ingredients; j++){ %>
                            <li><%= ingredients[i].ingredients %></li>
                        <% } %>
                    </ul>
                </div>
            <% } %>
            </div>
        <% } %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>